//! `mont` — the Montague command-line interface.
//!
//! Subcommands: `parse`, `lower`, `query`. Renders diagnostics via
//! `ariadne` and `color-eyre`.
//!
//! Populated in milestone M8.

fn main() {
    eprintln!("mont: not yet implemented (see milestone M8)");
    std::process::exit(2);
}
